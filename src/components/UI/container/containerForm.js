import React from 'react';
import '../container/containerForm.css';

const ContainerForm = ({ children }) => (
    <div className="container-form">
        {children}
    </div>
);

export default ContainerForm;